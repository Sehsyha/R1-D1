import { DocumentModule } from './DocumentModule'

describe('DocumentModule', () => {
  let documentModule: DocumentModule

  describe('initial state', () => {
    it('should initialize documents state to an empty array', () => {
      expect(documentModule.documents).toEqual([])
    })
  })

  describe('create', () => {
    it('should throw an error when category is not found', () => {

    })

    it('should throw an error when organism is not found', () => {

    })

    it('should return created document when parameters are valid', () => {

    })
  })
})
